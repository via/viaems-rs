[package]
name = "viaems"
version = "0.1.0"
edition = "2024"

[dependencies]
viaems-interface-derive = { path = "../viaems-interface-derive" }
clap = { version = "4.4", features = ["derive"] }
duckdb = { version = "1.4", features = ["bundled"] }
ctrlc = "3.4"
prost = "0.14.3"
cobs = "0.5.0"
crc = "3.4.0"

[build-dependencies]
prost-build = "0.14.3"
